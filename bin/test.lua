function (_ENV)
local string, math, print, type, pairs = nil, nil, nil, nil, nil;
local _module = _obj({exports=_obj({})}); local exports, module = _module.exports, _module;

local i, a = i, a;
if console:log(("hi"), Math:random()) then end;
i = (0);
if setInterval(global, (function (this)
local a = _debug.getinfo(1, 'f').func;
if console:log(("This will run on next event loop."), collectgarbage(global, ("count"))) then end;
end), (0)) then end;

return _module.exports;
end